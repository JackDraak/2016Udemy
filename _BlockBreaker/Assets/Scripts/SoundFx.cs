using UnityEngine;
using System.Collections;

public class SoundFx : MonoBehaviour {
/*		
	static SoundFx instance = null;
		
	public AudioClip[] effect_;
		
	private AudioSource soundFx;
		
	void Start () {
		if (instance != null && instance != this) {
			Destroy (gameObject);
			print ("Duplicate SoundFx self-destructing!");
		} else {
			instance = this;
			GameObject.DontDestroyOnLoad(gameObject);
			soundFx = GameObject.Find ("SoundFx").GetComponent<AudioSource>();
		}
	}
		
	void OnLevelWasLoaded(int level){
		if (music) {
			music.Stop();
			music.clip = level_[level];
//			if (level == 0) music.clip = null; // splash
//			if (level == 1) music.clip = startClip;
			music.loop = true;
			music.volume = 0.2f;
			music.Play();
		}
	} */
}
